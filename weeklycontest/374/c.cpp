#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    int countCompleteSubstrings(string word, int k) {
        int n = word.length();
        map<vector<int>, int> mp;
        vector<int> cnt(26);
        int pre = 0;

    }
};